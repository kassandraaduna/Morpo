import { View, Text } from 'react-native';

export default function Scan() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>SCAN (PLACEHOLDER)</Text>
    </View>
  );
}
