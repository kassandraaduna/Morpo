import { View, Text } from 'react-native';

export default function Models() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>3D Models (PLACEHOLDER)</Text>
    </View>
  );
}
