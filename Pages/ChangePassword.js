import { View, Text } from 'react-native';

export default function ChangePassword() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>CHANGE PASSWORD IN PROFILE (PLACEHOLDER)</Text>
        </View>
    );
}
